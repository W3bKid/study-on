{% extends 'base.html.twig' %}

{% block title %}Регистрация{% endblock %}

{% block body %}

    {% if error %}
        <div class="alert alert-danger">{{ error }}</div>
    {% endif %}

    <h1>Регистрация</h1>

    {{ include('security/_form_register.html.twig') }}

{% endblock %}
